<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <queries>
        <package android:name="com.google.android.apps.maps" />
        <!-- Add more packages as needed -->
    </queries>

    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyA582883yurIhpxcYWZcLr6b_a7tQMDPBI" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_g4unilink"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_g4unilink_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD24Sp_Group4UniLink"
        tools:targetApi="31">
        <activity
            android:name=".posts.EditAllPosts"
            android:exported="false" />
        <activity
            android:name=".posts.ViewAllPosts"
            android:exported="false" />
        <activity
            android:name=".posts.ViewPostActivity"
            android:exported="false" />
        <activity
            android:name=".posts.EditPost"
            android:exported="false" />
        <activity
            android:name=".posts.CreatePost"
            android:exported="false" />
        <activity
            android:name=".Register"
            android:exported="false" />
        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>